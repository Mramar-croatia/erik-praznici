rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Task states: anyone can read, only authenticated users can write
    match /state/tasks {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
